name: inventory_manager_api
description: API server for inventory_manager
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  mssql_connection: ^3.0.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  path: ^1.9.1
